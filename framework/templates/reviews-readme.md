# Reviews

Each completed review should produce one review report file.

Naming suggestion:
- `REVIEW-<nnn>-<task-or-slice>.md`

Rules:
- outcome must be `approved`, `rejected`, or `blocked`
- every defect should tie back to acceptance criteria, UX expectations, architecture constraints, or validation gaps
- Mission Control uses review reports to decide closure or rework
