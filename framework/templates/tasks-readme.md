# Tasks

Task files are the execution units for a project.

Rules:
- one bounded deliverable per task file
- keep status aligned with backlog and status log
- Builder updates validation and handoff fields before review
- Reviewer/QA writes a separate review report and does not overwrite task history
- Mission Control closes the loop after review
- task status must follow the framework lifecycle
